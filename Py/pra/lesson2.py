import builtins
builtins.print()

ranking = {
    'A': 100,
    'B': 85,
    'C': 95
}

# ranking.get('A')
print(sorted(ranking, key=ranking.get, reverse=True))

s = "teotaoienwgrse"

d = {}
for c in s:
    if c not in d

# 組み込み関数
# https://docs.python.org/ja/3/library/functions.html
